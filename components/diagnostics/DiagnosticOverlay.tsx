/**
 * Overlay de diagnóstico removido da UI principal.
 * Logs e erros ficam apenas no ecrã CentFlow Doctor (/settings/diagnostics).
 * O DiagnosticsBootstrap continua a capturar eventos em background.
 */
export function DiagnosticOverlay() {
  return null;
}
