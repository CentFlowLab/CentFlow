# Alias sideload — IPA unsigned apenas (sem OTA).
# Pipeline completo: CentFlow Release (release.yml).
name: Build iOS unsigned IPA

on:
  workflow_dispatch:

jobs:
  release:
    uses: ./.github/workflows/release.yml
    secrets: inherit
    with:
      publish_ota: false
      build_ipa: true
