/** UI de comparação anónima — inactiva por defeito até haver escala estatística. */
export const SPENDING_BENCHMARKS_UI_ENABLED =
  process.env.EXPO_PUBLIC_SPENDING_BENCHMARKS_UI === 'true';

export const MIN_BENCHMARK_SAMPLE_COUNT = 30;
